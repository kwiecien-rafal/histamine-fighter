/// <reference types="vite/client" />

// Injected by vite.config.ts: the date pricing.ts was last updated (its last git
// commit date, or today when the working tree has uncommitted price edits).
declare const __PRICES_UPDATED__: string;
